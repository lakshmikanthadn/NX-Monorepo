export interface TitleResponse {
  bookId: string;
  dacKey: string;
  doi: string;
  edition: string;
  title: string;
  format: string;
  isbn: string;
  publisherImprint: string;
  status: string;
}
